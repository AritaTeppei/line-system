①backend 起動
cd C:\dev\line-system\backend
npm run start:dev


②frontend 起動
cd C:\dev\line-system\frontend
npm run dev


③LINEWOOK
PS C:\dev\line-system\backend> cloudflared tunnel --url http://localhost:4000

④トークンの出し方
$body = @{
  email = "admin@example.com"
  password = "password123"
} | ConvertTo-Json

Invoke-RestMethod `
  -Method POST `
  -Uri "http://localhost:4000/auth/login" `
  -Headers @{ "Content-Type" = "application/json" } `
  -Body $body
